# 此文件只提供给 meilisearch 容器使用。
# 这里配置的是用于管理 keys 的 MEILI_MASTER_KEY。
# 浏览器 public search key 不属于这里；请把 Keys API 返回的真实 key 写入应用 .env 的 MEILI_PUBLIC_SEARCH_API_KEY。
# 不要填 uid；uid 只是 key 的标识，不能用于鉴权。
MEILI_MASTER_KEY=your-master-key-here
